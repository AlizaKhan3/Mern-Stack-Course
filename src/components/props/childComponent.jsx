const ChildComponent = (props) => {
    return(
        <p>Hello its recap {props.paragraph}</p>
        // <h1 className="text-secondary-color">this is from Child Component {props.childPropName}</h1>
    )
}

export default ChildComponent;